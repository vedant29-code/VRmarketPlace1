<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>VRMarketPlace — All Markets, One Place</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&display=swap" rel="stylesheet">

  <!-- Enhanced CSS (moved/expanded) -->
  <style>
    :root{
      --bg:#071027;               /* deepest back */
      --bg-2:#0f1724;             /* card background */
      --card:#0b1220;             /* product cards */
      --accent-1:#6EE7B7;         /* mint */
      --accent-2:#60A5FA;         /* sky */
      --muted:#94a3b8;            /* muted text */
      --glass: rgba(255,255,255,0.04);
      --glass-2: rgba(255,255,255,0.02);
      --glass-3: rgba(255,255,255,0.03);
      --soft-shadow: 0 8px 30px rgba(2,6,23,0.6);
      --radius-xl: 18px;
      --radius-md: 12px;
      --transition: 240ms cubic-bezier(.2,.9,.3,1);
      --glass-border: rgba(255,255,255,0.03);
    }

    /* Reset + base */
    *{box-sizing:border-box}
    html,body{height:100%;font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,'Helvetica Neue',Arial; background: radial-gradient(1200px 600px at 10% 10%, rgba(96,165,250,0.06), transparent 8%), linear-gradient(180deg,var(--bg) 0%, #05111e 100%); color:#e6eef8; margin:0;-webkit-font-smoothing:antialiased}
    img{display:block;max-width:100%}
    a{color:inherit;text-decoration:none}
    .container{max-width:1200px;margin:0 auto;padding:22px}

    /* Header */
    header{backdrop-filter: blur(8px); background:linear-gradient(90deg, rgba(255,255,255,0.02), transparent); position:sticky; top:0; z-index:50; border-bottom:1px solid var(--glass-border)}
    .nav{display:flex;gap:16px;align-items:center;justify-content:space-between}
    .brand{display:flex;align-items:center;gap:14px;font-weight:800}
    .logo{width:48px;height:48px;border-radius:12px;display:grid;place-items:center;background:linear-gradient(135deg,var(--accent-1),var(--accent-2)); color:#052024;font-weight:900;box-shadow:0 6px 18px rgba(96,165,250,0.08)}
    nav ul{display:flex;gap:14px;align-items:center;list-style:none;margin:0;padding:0}
    nav li{font-weight:600;color:var(--muted);padding:8px 12px;border-radius:10px;transition:background var(--transition), color var(--transition)}
    nav li:hover{background:rgba(255,255,255,0.02);color:#fff}

    /* Search */
    .search{flex:1;margin:0 18px;display:flex;align-items:center}
    .search input{width:100%;padding:12px 14px;border-radius:12px;border:1px solid var(--glass-border);background:linear-gradient(180deg,var(--glass),transparent);color:inherit;outline:none;transition:box-shadow var(--transition),transform var(--transition)}
    .search input:focus{box-shadow:0 8px 30px rgba(2,6,23,0.5);transform:translateY(-1px)}

    /* Hero */
    .hero{padding:44px 0;display:grid;grid-template-columns:1fr 420px;gap:28px;align-items:center}
    .hero-card{background:linear-gradient(180deg, rgba(255,255,255,0.02), transparent);border-radius:var(--radius-xl);padding:32px;border:1px solid var(--glass-border);box-shadow:0 12px 40px rgba(2,6,23,0.45)}
    .eyebrow{display:inline-block;background:linear-gradient(90deg, rgba(110,231,183,0.07), rgba(96,165,250,0.05));color:var(--accent-1);padding:8px 14px;border-radius:999px;font-weight:800;margin-bottom:18px}
    h1{font-size:40px;line-height:1.04;margin:0 0 10px}
    p.lead{color:var(--muted);margin:0 0 20px;font-size:15px}
    .cta{display:flex;gap:12px}

    .btn{padding:12px 18px;border-radius:12px;border:0;cursor:pointer;font-weight:800;transition:all var(--transition);display:inline-flex;align-items:center;gap:8px}
    .btn-primary{background:linear-gradient(90deg,var(--accent-1),var(--accent-2));color:#052024;box-shadow:0 8px 20px rgba(96,165,250,0.12);}
    .btn-primary:hover{transform:translateY(-4px);box-shadow:0 18px 50px rgba(96,165,250,0.14)}
    .btn-ghost{background:transparent;border:1px solid rgba(255,255,255,0.04);color:var(--muted)}
    .btn-ghost:hover{background:rgba(255,255,255,0.02);color:#fff}

    /* Categories */
    .categories{display:flex;gap:12px;margin-top:22px;flex-wrap:wrap}
    .cat{background:linear-gradient(180deg,rgba(255,255,255,0.02),transparent);padding:10px 14px;border-radius:999px;font-weight:700;color:var(--muted);display:flex;gap:10px;align-items:center;border:1px solid rgba(255,255,255,0.02)}

    /* Top right cards */
    .card{background:var(--card);border-radius:14px;padding:14px;border:1px solid var(--glass-border);transition:transform var(--transition), box-shadow var(--transition)}
    .card:hover{transform:translateY(-8px);box-shadow:var(--soft-shadow)}

    /* Product grid */
    .grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:18px;margin-top:28px}
    .product-card{background:linear-gradient(180deg, rgba(255,255,255,0.01), transparent);border-radius:14px;padding:14px;border:1px solid rgba(255,255,255,0.03);transition:transform var(--transition),box-shadow var(--transition);display:flex;flex-direction:column}
    .product-card:hover{transform:translateY(-8px);box-shadow:0 18px 40px rgba(2,6,23,0.6)}
    .thumb{height:160px;border-radius:10px;background-size:cover;background-position:center;margin-bottom:12px;flex-shrink:0}
    .meta{display:flex;justify-content:space-between;align-items:center}
    .price{font-weight:900}
    .rating{color:var(--muted);font-weight:700}
    .card-actions{margin-top:12px;display:flex;gap:8px}

    /* subtle badges and flags */
    .badge{background:linear-gradient(90deg, rgba(110,231,183,0.08), rgba(96,165,250,0.04));color:var(--accent-1);padding:6px 8px;border-radius:8px;font-weight:800}
    .pill{background:rgba(255,255,255,0.03);padding:6px 10px;border-radius:999px;font-weight:700}

    /* Drawer */
    .drawer{position:fixed;right:18px;bottom:18px;background:linear-gradient(180deg, rgba(255,255,255,0.02), transparent);padding:12px;border-radius:14px;border:1px solid rgba(255,255,255,0.04);backdrop-filter: blur(6px);box-shadow:0 12px 40px rgba(2,6,23,0.45)}

    /* Footer */
    footer{padding:28px 0;margin-top:28px;border-top:1px solid rgba(255,255,255,0.03)}
    .footer-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:18px}
    .small{color:var(--muted);font-size:13px}

    /* micro animations */
    .floating{animation:floaty 6s ease-in-out infinite}
    @keyframes floaty{0%{transform:translateY(0)}50%{transform:translateY(-6px)}100%{transform:translateY(0)}}

    /* responsive */
    @media (max-width:960px){.hero{grid-template-columns:1fr;}.search{display:none}.footer-grid{grid-template-columns:repeat(2,1fr)}}
    @media (max-width:520px){h1{font-size:24px}.grid{grid-template-columns:repeat(auto-fit,minmax(180px,1fr))}.logo{width:40px;height:40px}}

    /* accessibility helpers */
    [aria-live]{outline-offset:4px}

    /* utility */
    .muted{color:var(--muted)}
  </style>
</head>
<body>
  <header>
    <div class="container nav">
      <div style="display:flex;align-items:center;gap:12px">
        <div class="logo">VR</div>
        <div class="brand">VRMarketPlace <div style="font-weight:400;color:var(--muted);font-size:12px;margin-top:-4px">All markets, one place</div></div>
      </div>

      <div class="search" aria-hidden>
        <input id="searchInput" placeholder="Search products, brands, sellers..." />
      </div>

      <nav>
        <ul>
          <li>Explore</li>
          <li>Deals</li>
          <li>Stores</li>
          <li class="pill">For Sellers</li>
        </ul>
      </nav>
    </div>
  </header>

  <main class="container">
    <section class="hero">
      <div class="hero-card">
        <span class="eyebrow">New — Unified Marketplace</span>
        <h1>VRMarketPlace — Shop from every marketplace in one beautiful place</h1>
        <p class="lead">Curated collections, trusted sellers, and seamless checkout. Discover trending deals across electronics, fashion, home & more.</p>
        <div class="cta">
          <button class="btn btn-primary" onclick="scrollToGrid()">Start shopping</button>
          <button class="btn btn-ghost" onclick="openDemo()">How it works</button>
        </div>

        <div class="categories" aria-hidden>
          <div class="cat">🔌 Electronics</div>
          <div class="cat">👗 Fashion</div>
          <div class="cat">🏠 Home</div>
          <div class="cat">📚 Books</div>
          <div class="cat">🏀 Sports</div>
        </div>
      </div>

      <div style="display:flex;flex-direction:column;gap:12px">
        <div class="card" style="padding:18px;display:flex;flex-direction:column;gap:12px;align-items:flex-start">
          <div style="display:flex;justify-content:space-between;width:100%">
            <div>
              <div style="font-weight:800">Today’s Top Deal</div>
              <div class="small">Limited time: extra 10% off</div>
            </div>
            <div class="badge">Hot</div>
          </div>
          <div class="thumb floating" style="width:100%;height:220px;background-image:url('https://images.unsplash.com/photo-1542291026-7eec264c27ff?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&q=60&w=800');"></div>
          <div style="display:flex;justify-content:space-between;width:100%">
            <div>
              <div style="font-weight:800">Wireless Headphones</div>
              <div class="small">by SoundLab • free delivery</div>
            </div>
            <div style="text-align:right">
              <div class="price">₹2,499</div>
              <div class="rating">⭐ 4.7</div>
            </div>
          </div>
        </div>

        <div class="card" style="padding:16px;display:flex;gap:12px;align-items:center">
          <img src="https://images.unsplash.com/photo-1526178613004-6c2a0e6f58a6?crop=entropy&cs=tinysrgb&fit=crop&fm=jpg&q=60&w=120&h=120" alt="seller" style="width:64px;height:64px;border-radius:12px;object-fit:cover">
          <div>
            <div style="font-weight:800">Top seller: HomeStyle</div>
            <div class="small">Daily curated picks for your home</div>
          </div>
        </div>
      </div>
    </section>

    <section>
      <h2 style="margin-top:8px">Trending products</h2>
      <div id="productGrid" class="grid" aria-live="polite">
        <!-- Product cards (populated by JS) -->
      </div>
    </section>

    <section style="margin-top:26px">
      <div class="card" style="padding:18px;display:flex;gap:12px;align-items:center">
        <div style="flex:1">
          <div style="font-weight:800">Why VRMarketPlace?</div>
          <p class="small" style="margin-top:6px">We gather verified listings from multiple marketplaces, normalize prices & reviews, and give you a single cart and checkout — less hopping between sites, more smart decisions.</p>
        </div>
        <div style="min-width:220px;text-align:right">
          <div style="font-weight:800">Unified Checkout</div>
          <div class="small">Pay once, receive from multiple sellers</div>
        </div>
      </div>
    </section>

  </main>

  <div class="drawer" role="dialog" aria-label="Cart">
    <div style="font-weight:800">Cart <span style="color:var(--muted);font-weight:600;margin-left:6px">0 items</span></div>
    <div style="margin-top:8px"><button class="btn btn-primary" style="width:100%" onclick="alert('Demo checkout — no real payment in this template')">Checkout</button></div>
  </div>

  <footer>
    <div class="container">
      <div class="footer-grid">
        <div>
          <div style="font-weight:800">VRMarketPlace</div>
          <div class="small" style="margin-top:8px">All markets, one place — built with ❤️</div>
        </div>
        <div>
          <div style="font-weight:800">Company</div>
          <div class="small">About • Careers • Press</div>
        </div>
        <div>
          <div style="font-weight:800">Support</div>
          <div class="small">FAQ • Shipping • Returns</div>
        </div>
        <div>
          <div style="font-weight:800">Follow</div>
          <div class="small">Twitter • Instagram • LinkedIn</div>
        </div>
      </div>

      <div style="margin-top:18px;text-align:center" class="small">© <span id="year"></span> VRMarketPlace — All rights reserved.</div>
    </div>
  </footer>

  <script>
    // small product dataset (placeholder). For production replace with API calls.
    const products = [
      {id:1,title:'Smartwatch Series Z',price:7499,thumb:'https://images.unsplash.com/photo-1511707171634-5f897ff02aa9?auto=format&fit=crop&w=800&q=60',rating:4.5},
      {id:2,title:'Minimal Leather Wallet',price:899,thumb:'https://images.unsplash.com/photo-1555529771-5b5b4f0d6b31?auto=format&fit=crop&w=800&q=60',rating:4.6},
      {id:3,title:'Gaming Mouse',price:1999,thumb:'https://images.unsplash.com/photo-1587202372775-8648b2b5d6b5?auto=format&fit=crop&w=800&q=60',rating:4.4},
      {id:4,title:'Noise Cancelling Earbuds',price:2499,thumb:'https://images.unsplash.com/photo-1585386959984-a415522a3f32?auto=format&fit=crop&w=800&q=60',rating:4.7},
      {id:5,title:'Organic Cotton T-Shirt',price:699,thumb:'https://images.unsplash.com/photo-1541099649105-f69ad21f3246?auto=format&fit=crop&w=800&q=60',rating:4.2},
      {id:6,title:'Desk Lamp Smart',price:1299,thumb:'https://images.unsplash.com/photo-1505691723518-36a6a4700c6f?auto=format&fit=crop&w=800&q=60',rating:4.3},
      {id:7,title:'Bestseller Novel',price:349,thumb:'https://images.unsplash.com/photo-1512820790803-83ca734da794?auto=format&fit=crop&w=800&q=60',rating:4.9},
      {id:8,title:'Yoga Mat Pro',price:1199,thumb:'https://images.unsplash.com/photo-1558611848-73f7eb4001d8?auto=format&fit=crop&w=800&q=60',rating:4.5}
    ];

    function renderGrid(list){
      const grid = document.getElementById('productGrid');
      grid.innerHTML='';
      list.forEach(p=>{
        const card = document.createElement('div'); card.className='product-card';
        card.innerHTML = `
          <div class="thumb" style="background-image:url('${p.thumb}')"></div>
          <div style="display:flex;justify-content:space-between;align-items:center">
            <div>
              <div style="font-weight:800">${p.title}</div>
              <div class="small muted">by seller • free delivery</div>
            </div>
            <div style="text-align:right">
              <div class="price">₹${p.price.toLocaleString('en-IN')}</div>
              <div class="rating">⭐ ${p.rating}</div>
            </div>
          </div>
          <div class="card-actions">
            <button class="btn btn-primary" style="flex:1" onclick="addToCart(${p.id})">Add to cart</button>
            <button class="btn btn-ghost" onclick="viewDetails(${p.id})">View</button>
          </div>
        `;
        grid.appendChild(card);
      });
    }

    function addToCart(id){
      const product = products.find(p=>p.id===id);
      alert(product.title + ' added to cart — demo only');
    }
    function viewDetails(id){
      const product = products.find(p=>p.id===id);
      alert(product.title + '

Price: ₹' + product.price + '
Rating: ' + product.rating + '

This is a static demo card.');
    }

    function scrollToGrid(){
      document.getElementById('productGrid').scrollIntoView({behavior:'smooth'});
    }
    function openDemo(){
      alert('VRMarketPlace connects multiple marketplaces, normalizes listings and offers one checkout. Use this template as a starting UI.');
    }

    // Search
    document.getElementById('searchInput').addEventListener('input', (e)=>{
      const q = e.target.value.trim().toLowerCase();
      if(!q){ renderGrid(products); return }
      const filtered = products.filter(p=>p.title.toLowerCase().includes(q));
      renderGrid(filtered);
    });

    // initial render
    renderGrid(products);
    document.getElementById('year').textContent = new Date().getFullYear();
  </script>
</body>
</html>
